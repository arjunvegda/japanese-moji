import type { CharacterDict, CharacterSet } from '../types';
export declare const createRegexGroups: (characterSetMap: CharacterDict, characterSets: CharacterSet[], customRanges: string[]) => string;
